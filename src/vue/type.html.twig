{% extends "base.html.twig" %}

{% block title %} {{ parent() }} Type {% endblock %}
{% block contenu %}

<div class="table-responsive">
    <table class="table">
        <thead>
        <tr>
            <th scope="col">id</th>
            <th scope="col">nom</th>
        </tr>
        </thead>
        <tbody>
{% for t in liste %}

<tr>
    <td>{{t.id}}</td>
    <td>{{t.nom}}</td>
</tr>

{% endfor %}
        </tbody>
    </table>
</div>

</br></br></br></br></br>
<form action="index.php?page=type"method="post"enctype="application/x-www-form-urlencoded">
    <label for="inputType" class="sr-only">Ajouter un type :</label>
    <input type="nom" id="inputType" name="inputType" class="form-control" placeholder="Type" required>
    <button class="btn btn-lg btn-primary btn-block" type="submit" name="validType">Valider</button>
</form>

{% endblock %}